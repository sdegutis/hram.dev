#include <optional>
#include <vector>
#include <string>

//#define tutvermaj @hram_VERSION_MAJOR@
//#define tutvermin @hram_VERSION_MINOR@

std::optional<std::vector<uint8_t>> parse_wat(std::string input);
